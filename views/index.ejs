<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Myora | Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">


    <style>
        /* Color Scheme Variables */
        :root {
            --deep-navy: #1a1f36;
            --vibrant-teal: #00bfa6;
            --warm-coral: #ff6b6b;
            --light-ivory: #f9f9f9;
            --soft-gray: #e0e0e0;
        }

        body {
            background-color: var(--light-ivory);
            font-family: 'Segoe UI', sans-serif;
        }

        /* Navbar */
        .navbar {
            background-color: var(--deep-navy) !important;
        }

        .navbar-brand img {
            height: 40px;
            margin-right: 10px;
        }

        .navbar-brand,
        .nav-link {
            color: var(--light-ivory) !important;
        }

        .nav-link:hover {
            color: var(--vibrant-teal) !important;
        }

        /* Banner Modern Glass Layout */
        .banner {
            position: relative;
            background: linear-gradient(rgba(26, 31, 54, 0.6), rgba(0, 191, 166, 0.4)),
                url('/images/banner-bg.jpg') no-repeat center center/cover;
            height: 450px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .banner-content {
            display: flex;
            align-items: center;
            gap: 40px;
            max-width: 1100px;
            width: 100%;
            backdrop-filter: blur(15px);
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        /* Banner Text */
        .banner-text {
            flex: 1;
            color: var(--light-ivory);
        }

        .banner-text h1 {
            font-size: 2.8rem;
            font-weight: bold;
        }

        .banner-text p {
            font-size: 1.2rem;
            margin-top: 10px;
        }

        .banner-text .btn {
            margin-top: 20px;
            background: linear-gradient(135deg, var(--vibrant-teal), var(--warm-coral));
            border: none;
            padding: 12px 28px;
            font-size: 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .banner-text .btn:hover {
            background: linear-gradient(135deg, var(--warm-coral), var(--vibrant-teal));
            transform: translateY(-3px) scale(1.03);
        }

        /* Banner Image Floating */
        .banner-image {
            flex: 1;
            display: flex;
            justify-content: center;
        }

        .banner-image img {
            max-width: 320px;
            width: 100%;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            animation: floatImage 4s ease-in-out infinite;
        }

        @keyframes floatImage {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        /* Product Cards */
        .product-card {
            border: 1px solid var(--soft-gray);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(6px);
        }

        .product-card img {
            height: 200px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-card:hover img {
            transform: scale(1.05);
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 18px rgba(0, 0, 0, 0.15);
        }

        .btn-primary {
            background-color: var(--vibrant-teal);
            border-color: var(--vibrant-teal);
        }

        .btn-primary:hover {
            background-color: var(--warm-coral);
            border-color: var(--warm-coral);
        }

        /* Footer */
        footer {
            background-color: var(--deep-navy);
            color: var(--light-ivory);
            padding: 40px 0;
        }

        footer a {
            color: var(--light-ivory);
            text-decoration: none;
        }

        footer a:hover {
            color: var(--vibrant-teal);
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
                <img src="/images/E-Commerce.png" alt="Myora Logo" height="100px"> Myora
            </a>
            <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/productList">Shop</a></li>
                    <li class="nav-item"><a class="nav-link" href="/aboutus">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">Hi, <%= user.name %></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Logout</a>
                        </li>
                        <% } else { %>
                            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                            <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Banner -->
    <div class="banner">
        <div class="banner-content" data-aos="fade-up">
            <div class="banner-text" data-aos="fade-right">
                <h1>🔥 New Season Sale – Up to 50% OFF!</h1>
                <p>Discover premium gadgets, fashion, and home essentials at unbeatable prices.</p>
                <a href="/shop" class="btn btn-primary">Shop Now</a>
            </div>
            <div class="banner-image" data-aos="fade-left">
                <img src="/images/iphone.jpg" alt="Featured Product">
            </div>
        </div>
    </div>

    <!-- Products Showcase -->
    <section class="container my-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 style="color: var(--deep-navy);" data-aos="zoom-in">Our Top Picks</h2>
            <a href="/shop" class="btn btn-outline-primary"
                style="border-color: var(--vibrant-teal); color: var(--vibrant-teal);" data-aos="fade-left">
                View More →
            </a>
        </div>

        <% let products=[ { name: "iPhone 16" , price: "Rs 82,900 INR" , img: "/images/Phone.jpg" }, {
            name: "OXVIEW LED TV" , price: "Rs 20,600.00 INR" , img: "/images/tv.jpg" }, {
            name: "LG 9 Kg 5 star Wifi Inverter Fully Automatic" , price: "Rs 20,000 INR" ,
            img: "/images/washing machine.jpg" }, { name: "The ALCHEMIST" , price: "Rs 340.00 INR" ,
            img: "/images/book.jpg" }, { name: "PlayStation 5" , price: "Rs 54,900.00 INR" , img: "/images/ps.jpg" }, {
            name: "Apple AirPods 4 Wireless Earbuds" , price: "Rs 199.00 INR" , img: "/images/airpods.jpg" } ]; %>
            <div class="row justify-content-center text-center g-4">
                <% products.forEach((p, index)=> { %>
                    <div class="col-6 col-sm-4 col-md-2 d-flex" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                        <div class="card product-card shadow-sm w-100">
                            <img src="<%= p.img %>" class="card-img-top" alt="<%= p.name %>">
                            <div class="card-body">
                                <h5 class="card-title" style="color: var(--deep-navy);">
                                    <%= p.name %>
                                </h5>
                                <p class="card-text fw-bold" style="color: var(--warm-coral);">
                                    <%= p.price %>
                                </p>
                                <!-- <a href="/product/<%= p.name %>" class="btn btn-primary btn-sm">View</a> -->
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row text-center text-md-start">
                <div class="col-md-4 mb-3">
                    <h5>Myora</h5>
                    <p>Your go-to store for the latest gadgets, fashion, and home essentials.</p>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="/">Home</a></li>
                        <li><a href="/shop">Shop</a></li>
                        <li><a href="/aboutus">About</a></li>
                        <li><a href="/contact">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h5>Contact Us</h5>
                    <p>Email: realtime@myora.com</p>
                    <p>Phone: +91 9876543210</p>
                    <p><i class="fa-brands fa-facebook"></i>  <i class="fa-brands fa-instagram"></i>  <i class="fa-brands fa-twitter"></i></p>
                    
                </div>

            </div>
            <hr class="border-light">
            <p class="text-center mb-0">© 2025 Myora. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ duration: 1000, once: true });
    </script>
</body>

</html>
